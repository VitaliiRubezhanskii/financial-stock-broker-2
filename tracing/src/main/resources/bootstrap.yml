server:
  port: ${PORT}

spring:
  application:
    name: tracing
  cloud:
    config:
      uri: ${CONFIGSERVER_URI}}

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    serviceUrl:
      defaultZone: http://discovery:8761/eureka/



