server:
  port: ${PORT}
spring:
  application:
    name: feign
  spring:
    profiles:
      active: ${SPRING_PROFILES_ACTIVE}
  cloud:
     config:
       uri: ${CONFIGSERVER_URI}
  main:
    allow-bean-definition-overriding: true





