spring:
  profiles:
    active: ${SPRING_PROFILES_ACTIVE}

  application:
    name: finprovider

  cloud:
    config:
      uri: ${CONFIGSERVER_URI}
  main:
    allow-bean-definition-overriding: true
server:
  port: ${PORT}



