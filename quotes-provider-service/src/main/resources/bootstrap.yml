spring:
  application:
    name: quotes-provider-service

management:
  context-path: /admin
  security:
    enabled: false
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    refresh:
      enabled: true







