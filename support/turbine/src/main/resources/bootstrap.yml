spring:
  application:
    name: turbine-service
  cloud:
    config:
      retry:
        initialInterval: 3000
        multiplier: 1.3
        maxInterval: 5000
        maxAttempts: 20
      uri: http://localhost:8888
      discovery:
        serviceId: config-server
        enabled: false
