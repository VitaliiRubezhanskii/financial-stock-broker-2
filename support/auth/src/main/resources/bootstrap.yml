spring:
  profiles:
    active: ${SPRING_PROFILES_ACTIVE}
  application:
    name: auth
  cloud:
    config:
      uri: ${CONFIGSERVER_URI}

server:
  port: ${PORT}
  servlet:
    context-path: /uaa



