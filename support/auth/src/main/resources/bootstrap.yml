server:
  port: 8100
spring:
  application:
    name: auth-service
  cloud:
    config:
      uri: http://configserver:8888

custom:
  oauth:
    tokenId: tokenId
    refresh-token: refreshToken
    authId: authenticationId
    clientId: clientId
    username: username



