spring:
  profiles:
    active: ${SPRING_PROFILES_ACTIVE}

  application:
    name: analytics

  cloud:
    config:
      uri: ${CONFIGSERVER_URI}
  main:
    allow-bean-definition-overriding: true
#server:
#  port: ${PORT}