spring:
  data:
    mongodb:
     uri: mongodb://user:user@localhost:27018,localhost:27019,localhost:27020/auth?replicaSet=rs0
      #host: localhost
      #port: 27017
      #username: root
      #password: root
      #database: admin

server:
  servlet:
    context-path: /uaa
