spring:
  data:
    mongodb:
     uri: mongodb://root:root@mongo1:27017,mongo2:27017,mongo3:27017/auth?replicaSet=rs0
      #host: localhost
      #port: 27017
      #username: root
      #password: root
      #database: admin

server:
  servlet:
    context-path: /uaa
