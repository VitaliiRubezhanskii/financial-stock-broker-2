spring:
  data:
    mongodb:
      host: mongodb-service.default.svc.cluster.local
      port: 27017
      username: username
      password: password
      database: auth
      authentication-database: admin

server:
  servlet:
    context-path: /uaa
