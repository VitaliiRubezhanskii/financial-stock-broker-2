server:
  port: 8081
spring:
  application:
    name: account_service
    #account-service
  cloud:
    config:
      uri: http://localhost:8888
      #http://localhost:8888
      label: master
      #master
      profile: dev
      #dev

