server:
  port: 8081
spring:
  application:
    name: account_service
    #account-service
  cloud:
    config:
      uri: http://config_service:8888
      #http://localhost:8888
      label: docker
      #master
      profile: dev
      #dev

