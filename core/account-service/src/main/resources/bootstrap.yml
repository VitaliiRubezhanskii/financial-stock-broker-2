spring:
  application:
    name: ${APPLICATION_NAME}
    #account-service
  cloud:
    config:
      uri: ${CONFIG_SERVICE_URI}
      #http://localhost:8888
      label: ${BRANCH}
      #master
      profile: ${PROFILE}
      #dev

