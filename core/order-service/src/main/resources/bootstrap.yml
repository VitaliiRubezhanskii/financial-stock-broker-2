
spring:
  application:
    name: ${ORDER_SERVICE_NAME}
  profiles:
    active: default
     #order-service
  cloud:
    config:
      uri: ${CONFIG_SERVICE_URI}
       #http://localhost:8888
#      label: ${BRANCH}
      #master
      profile: ${PROFILE}
      #dev

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URI}
server:
  port: ${PORT}
      #http://localhost:8761/eureka/




